+ **Enable Lower Case Table Names enables the feature on existing service instances:**
If a developer selects **Enable Lower Case Table Names** in the tile,
the feature is automatically enabled on existing service instances.
This can cause existing tables with uppercase names to become orphaned.
