* **Uppercase letters in the service network name blocks Multi-Site Replication setup:**
  BOSH DNS can only resolve lowercase letters.
  Because the service network name is only used to
  reference the network configuration in BOSH, change the network name to lowercase letters.
