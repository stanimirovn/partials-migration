* **New service instances cannot be configured for Multi-Site Replication with <%= vars.app_runtime_abbr %> v2.10:**
      This does not affect existing service instances that are already configured for Multi-Site Replication. Other service plan topologies are also unaffected.