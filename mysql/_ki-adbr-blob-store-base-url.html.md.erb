* **ApplicationDataBackupRestore (adbr) fails to list backups when the container name is omitted from the Blob Store Base URL:**
As a workaround, the **Blob Store Base URL** must follow the format: `my-storage-account.my-custom.domain/MY-CONTAINER-NAME`.
