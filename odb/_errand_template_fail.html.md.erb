* The `upgrade-all-service-instances` errand fails during templating when the broker is not configured with Cloud Foundry. For instructions for configuring the broker, see [Write a Broker Manifest](operating.html#broker-manifest).
