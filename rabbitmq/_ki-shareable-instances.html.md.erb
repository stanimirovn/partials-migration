* Shareable instances are enabled in the **Global Settings for On-Demand Plans** tab
  for the pre-provisioned service as well as the on-demand service.
  For how to enable shareable instances, see [Configure Global Settings](./install-config.html#global).
