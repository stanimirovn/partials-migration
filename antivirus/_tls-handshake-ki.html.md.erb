<%# Still occurs as of 2.2.2 %>

* <%= vars.product_short_mirror %> outputs a TLS handshake error log
  even though the process is not failing.
  The error looks similar to the following:
      <pre class="terminal">http: TLS handshake error from 127.0.0.1:RANDOM-PORT: EOF</pre>
