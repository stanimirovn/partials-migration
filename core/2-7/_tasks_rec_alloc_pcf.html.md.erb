You configure the default memory and disk allocations using the **Default app memory** and **Default disk quota per app** fields in the **App Developer Controls** pane of the <%= vars.app_runtime_full %> (<%= vars.app_runtime_abbr %>) tile.
