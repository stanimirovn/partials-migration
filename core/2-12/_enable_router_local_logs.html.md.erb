(Optional) The **Enable the Gorouter to write access logs locally** checkbox is selected by default. <%= vars.company_name %> recommends disabling this checkbox for high-traffic deployments, since logs may not be rotated fast enough and can fill up the disk.
